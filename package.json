{
  "name": "vanilla-basic",
  "version": "1.0.0",
  "scripts": {
    "start": "npx sirv ./src public --cors --single --no-clear --port 8000 --host 0.0.0.0"
  },
  "devDependencies": {
    "sirv-cli": "^3.0.1"
  }
}
